# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!

def shared_pods
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
end

target 'MarkedMap' do
  shared_pods
  pod 'GoogleMaps'
end
  
target 'MarkedMapCore' do
  shared_pods
end
